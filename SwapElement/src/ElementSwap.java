/**
 * Array can be sorted by repeatedly swapping elements that are not in order
 */

/**
 * @author LalaDread
 *
 */
public class ElementSwap {
	
	//consider the task of swapping the elements at positions i and j of an array values
	//We'd like to set values[i] to values[j]
	
	//double temp = values[i];
	//values[i] = values[j];
	
	//Now we set values[j] to the saved value
	//values[j] = temp;

}
