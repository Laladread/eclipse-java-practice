import java.util.ArrayList;

/**
 * When you collect an unknown number of inputs, array lists are much easier to use than
 * arrays. Simply read inputs and add them to an array list
 */

/**
 * @author LalaDread
 *
 */
public class InputValue {
	ArrayList<Double> inputs = new ArrayList<Double>();
	while(in.hasNextDouble());
	{
		inputs.add(in.nextDouble());
	}

}
