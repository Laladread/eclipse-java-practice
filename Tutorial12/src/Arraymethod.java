//The following method computes the sum of an array of floating-point numbers


public static double sum(double[] values)
{
	double total = 0;
	for(double element : values)
	{
		total = total + element;
	}
	return total;
}
public class Arraymethod {

	public static void main(String[] args) {
	//Arrays can occur as method arguments and return values
		
//When you define a method with an array argument, you provide a 
		//parameter variable for the array	
		
		
		//the following method multiplies all elements of an array by a given factor
		
		public static void multiply(double[] values, double factor);
		{
			for(int i = 0; i < values.length; i++)
			{
				values[i] = values[i] * factor;
			}
		}
		
		
	}

}
