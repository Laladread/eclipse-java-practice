/**
 * If the order  of the element does not matter, you can simply insert new elements at the
 * end incrementing the variable tracking the size
 */

/**
 * @author LalaDread
 *
 */
public class ElementInsert {
	//if(currentSize < values.length)
	//{
	//currentSize++;
	//values[currentSize - 1] = newElement;
	//}
	
	/**
	 * It is more work to insert an element  at a particular position in the middle of an 
	 * array.
	 * When you insert an element, you start at the end of the array, move that element to 
	 * a higher index, then move the one before that, and so on
	 * 
	 * if(currentSize < values.length)
	 * {
	 * currentSize++;
	 * for(int i = currentSize - 1; i > pos; i--)
	 * {
	 * values[i] = values[i - 1];
	 * }
	 * values[pos] = newElement;
	 * }
	 */

}
